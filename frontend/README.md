# AL DALOUAA — Frontend

React + Vite. See the root `README.md` for the full setup.

```bash
cp .env.example .env   # set VITE_API_URL
npm install
npm run dev            # development
npm run build          # production build -> dist/
```
